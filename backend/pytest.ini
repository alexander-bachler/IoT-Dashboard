[pytest]
# Only collect the real test suite; keep helper scripts (e.g. scripts/smoke_test.py)
# out of collection even though their filename matches pytest's default patterns.
testpaths = tests
asyncio_mode = auto
